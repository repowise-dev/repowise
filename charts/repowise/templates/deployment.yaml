apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "repowise.fullname" . }}
  labels:
    {{- include "repowise.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "repowise.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "repowise.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "repowise.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.repos }}
      initContainers:
        - name: clone-repos
          image: bitnami/git:latest
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -e
              mkdir -p /data/repos
              {{- if or .Values.gitCredentials.secretName .Values.gitCredentials.github.token }}
              git config --global credential.helper 'store --file /tmp/.git-credentials'
              cp /git-credentials/git-credentials /tmp/.git-credentials
              chmod 600 /tmp/.git-credentials
              {{- end }}
              {{- range .Values.repos }}
              echo "=== {{ .name }} ==="
              if [ ! -d "/data/repos/{{ .name }}/.git" ]; then
                git clone --branch {{ .branch | default "main" }} --single-branch {{ .url }} "/data/repos/{{ .name }}"
              else
                cd "/data/repos/{{ .name }}"
                git fetch origin {{ .branch | default "main" }}
                git reset --hard origin/{{ .branch | default "main" }}
              fi
              {{- end }}
              echo "Done."
          volumeMounts:
            - name: data
              mountPath: /data
            {{- if or .Values.gitCredentials.secretName .Values.gitCredentials.github.token }}
            - name: git-credentials
              mountPath: /git-credentials
              readOnly: true
            {{- end }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: backend
              containerPort: {{ .Values.repowise.backendPort }}
              protocol: TCP
            - name: frontend
              containerPort: {{ .Values.repowise.frontendPort }}
              protocol: TCP
          env:
            - name: REPOWISE_DB_URL
              value: {{ .Values.repowise.dbUrl | quote }}
            - name: REPOWISE_EMBEDDER
              value: {{ .Values.repowise.embedder | quote }}
            - name: PORT_BACKEND
              value: {{ .Values.repowise.backendPort | quote }}
            - name: PORT_FRONTEND
              value: {{ .Values.repowise.frontendPort | quote }}
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "repowise.secretName" . }}
                  key: ANTHROPIC_API_KEY
                  optional: true
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "repowise.secretName" . }}
                  key: OPENAI_API_KEY
                  optional: true
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "repowise.secretName" . }}
                  key: GEMINI_API_KEY
                  optional: true
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- if .Values.repos }}
        - name: register-repos
          image: curlimages/curl:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              API="http://localhost:{{ .Values.repowise.backendPort }}"
              echo "Waiting for API..."
              until curl -sf "$API/health" > /dev/null 2>&1; do sleep 3; done
              echo "API ready!"
              {{- range .Values.repos }}
              echo "--- Registering {{ .name }} ---"
              curl -sf -X POST "$API/api/repos" \
                -H "Content-Type: application/json" \
                -d '{"name":"{{ .name }}","local_path":"/data/repos/{{ .name }}","url":"{{ .url }}","default_branch":"{{ .branch | default "main" }}"}' \
                && echo " registered" || echo " (exists)"
              REPO_ID=$(curl -sf "$API/api/repos" | sed -n 's/.*"id":"\([^"]*\)","name":"{{ .name }}".*/\1/p')
              if [ -n "$REPO_ID" ]; then
                curl -sf -X POST "$API/api/repos/$REPO_ID/sync" && echo " sync triggered" || echo " (sync running)"
              fi
              {{- end }}
              echo "All repos registered. Exiting."
        {{- end }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "repowise.fullname" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- if or .Values.gitCredentials.secretName .Values.gitCredentials.github.token }}
        - name: git-credentials
          secret:
            secretName: {{ .Values.gitCredentials.secretName | default (printf "%s-git-credentials" (include "repowise.fullname" .)) }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
