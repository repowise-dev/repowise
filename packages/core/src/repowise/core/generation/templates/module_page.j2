{% if ctx.is_rollup %}
You are documenting **{{ ctx.title }}**, an OVERVIEW of the `{{ ctx.directories[0] if ctx.directories else ctx.title }}` subsystem as a whole. Detail lives on the child pages listed below; this page situates the subsystem, explains how its parts fit together, and points a reader to the right child page. Do not re-document each file.
{% else %}
You are documenting **{{ ctx.title }}** (language: {{ ctx.language }}), a group of files that belong together, not a single directory. Write about what the group does as a whole.
{% endif %}

{% if ctx.scope %}
SCOPE (use this to open by situating the page against its siblings — say what it covers and what it deliberately leaves to other pages; do NOT quote this line verbatim):
> {{ ctx.scope }}
{% endif %}

## Material

**Covers:** {% if ctx.directories %}{% for d in ctx.directories[:6] %}`{{ d }}`{% if not loop.last %}, {% endif %}{% endfor %}{% if ctx.directories | length > 6 %} and {{ ctx.directories | length - 6 }} more{% endif %}{% else %}the repository root{% endif %} | **Language:** {{ ctx.language }} | **Files:** {{ ctx.files | length }} | **Public symbols:** {{ ctx.public_symbols }} / {{ ctx.total_symbols }}{% if ctx.community_label %} | **Community:** {{ ctx.community_label }}{% if ctx.community_cohesion > 0 %} (cohesion {{ "%.2f"|format(ctx.community_cohesion) }}){% endif %}{% endif %}

{%- set _mod_signals = [] -%}
{%- if ctx.entry_points -%}{%- set _ = _mod_signals.append("hosts entry points into this subsystem") -%}{%- endif -%}
{%- if ctx.pagerank_mean is defined and ctx.pagerank_mean >= 0.005 -%}{%- set _ = _mod_signals.append("architecturally central (high mean PageRank across its files)") -%}{%- endif -%}
{% if _mod_signals %}
**Architectural signals:** {{ _mod_signals | join("; ") }}. Weave these into the opening rather than listing them.
{% endif %}

{% if ctx.is_rollup and ctx.child_pages %}
### Child pages (this overview links down to these)
Name each in backticks by its path so it resolves to a wiki link; explain in one clause what each covers and when a reader should go there.
{% for cp in ctx.child_pages %}
- **{{ cp.title }}** — `{{ cp.path }}`
{% endfor %}
{% endif %}

{% if ctx.entry_points %}
### Entry points
{% for ep in ctx.entry_points %}
- `{{ ep }}`
{% endfor %}
{% endif %}

{% if ctx.key_files %}
### Key files (highest PageRank first — read these first)
{% for kf in ctx.key_files %}
- `{{ kf.path }}`{% if kf.is_entry_point %} *(entry point)*{% endif %}{% if kf.summary %} — {{ kf.summary }}{% endif %}
{% endfor %}
{% endif %}

{% if ctx.decision_records %}
### Architectural decisions
Use these to explain *why* the subsystem is shaped the way it is. Do not invent rationale beyond what is listed.
{% for dr in ctx.decision_records[:5] %}
- **{{ dr.title }}** — {{ dr.decision }}{% if dr.rationale %} *(Why: {{ dr.rationale }})*{% endif %}
{% endfor %}
{% endif %}

{% if ctx.dependencies %}
### Depends on (modules this imports)
{% for dep in ctx.dependencies[:25] %}
- `{{ dep }}`
{% endfor %}
{% endif %}

{% if ctx.dependents %}
### Depended on by (modules that import this)
{% for dep in ctx.dependents[:25] %}
- `{{ dep }}`
{% endfor %}
{% endif %}

{% if ctx.external_systems %}
### External systems used
{% for sys in ctx.external_systems[:15] %}
- `{{ sys.name }}`{% if sys.category and sys.category != 'library' %} *({{ sys.category }})*{% endif %}{% if sys.ecosystem %} — {{ sys.ecosystem }}{% endif %}
{% endfor %}
{% endif %}

{# Health and history signals a reader cannot get from the code alone. Only
   surfaced when git metadata supplied them; all zero/empty degrades to nothing
   rather than a fabricated number. This is repowise's differentiation — weave
   the true ones into the narrative, do not tabulate them mechanically. #}
{%- set _health = [] -%}
{%- if ctx.hotspot_count > 0 -%}{%- set _ = _health.append(ctx.hotspot_count ~ " file" ~ ("s" if ctx.hotspot_count != 1 else "") ~ " flagged as change hotspots") -%}{%- endif -%}
{%- if ctx.single_owner_files > 0 -%}{%- set _ = _health.append(ctx.single_owner_files ~ " file" ~ ("s" if ctx.single_owner_files != 1 else "") ~ " with a bus factor of one (single-maintainer risk)") -%}{%- endif -%}
{%- if ctx.bugfix_total > 0 -%}{%- set _ = _health.append(ctx.bugfix_total ~ " bug-fix commit" ~ ("s" if ctx.bugfix_total != 1 else "") ~ " in recent history" ~ (", most in `" ~ ctx.most_fixed_file.path ~ "`" if ctx.most_fixed_file else "")) -%}{%- endif -%}
{%- if ctx.stable_count > 0 -%}{%- set _ = _health.append(ctx.stable_count ~ " file" ~ ("s" if ctx.stable_count != 1 else "") ~ " that have been stable") -%}{%- endif -%}
{% if _health %}
### Subsystem health (from git history — state the notable ones plainly)
{% for h in _health %}
- {{ h }}
{% endfor %}
{% endif %}

{% if ctx.coupled_modules %}
### Change-coupled with (co-changes in history, not import edges)
These modules change together with this one in history without importing it — hidden coupling worth naming.
{% for cm in ctx.coupled_modules %}
- `{{ cm.path }}` (co-changed {{ cm.count }}×)
{% endfor %}
{% endif %}

{% if ctx.top_owners %}
### Ownership
{% for o in ctx.top_owners %}
- **{{ o.name }}** — primary maintainer of {{ o.file_count }} file(s)
{% endfor %}
{% endif %}

{% if ctx.key_classes %}
### Class hierarchy
{% for h in ctx.key_classes %}
- `{{ h.child }}` {{ h.kind }} `{{ h.parent }}`{% if h.parent_file is defined and h.parent_file %} (in `{{ h.parent_file }}`){% endif %}
{% endfor %}
{% endif %}

{% if ctx.dead_code_findings %}
### Potentially unused code
{% for finding in ctx.dead_code_findings[:10] %}
- `{{ finding.symbol_name or '?' }}` — {{ finding.reason }} ({{ (finding.confidence * 100) | int }}%{% if finding.safe_to_delete %}, cleanup-ready{% endif %})
{% endfor %}
{% endif %}

{% if module_git_summary is defined and module_git_summary %}
### Recent activity
Most active file: `{{ module_git_summary.most_active_file }}` ({{ module_git_summary.most_active_commits_90d }} commits in 90 days).
{% endif %}

## How to write this page

Write the page now from the material above. Requirements:

- Open with the role-leading opener described, situating this page against its siblings using the scope line. Do not begin with "This module contains N files".
- Choose H2/H3 headings that name this subsystem's real concerns. There is no fixed heading list — do not emit generic "Overview / Public API / Architecture Notes" headings unless they are genuinely the right names here.
- Synthesise across the whole file set. Draw on the material as a body of evidence; a file-by-file walkthrough is a failure even when accurate. Draw on many files, not one.
- Put any enumerable list (key files, dependencies, decisions) in a markdown table where it reads better than prose.
- Name the subsystem's key files and the neighbouring modules it consumes or serves in backticks exactly as written above so they resolve to wiki links. Mention a neighbour only where it explains this subsystem's role — no links for density, no reciprocal mentions for symmetry.
- Weave the true health and history signals (hotspots, bus factor, change coupling, fix history) into the narrative where they explain the subsystem's character. Skip any that are absent; never invent one.
- Third person throughout. No concluding or summary section.
- End with the mandatory `## Questions this page answers` section: 3 to 6 real questions this page answers, phrased as a developer or agent would ask them.

Ground everything in the material above. Do not fabricate files, symbols, modules, or rationale that are not listed.
