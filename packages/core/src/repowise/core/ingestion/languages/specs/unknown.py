"""LanguageSpec for unknown (extracted from the registry data table)."""

from ..spec import LanguageSpec

SPEC = LanguageSpec(
    tag="unknown",
    display_name="Unknown",
    is_code=False,
    is_passthrough=True,
)
