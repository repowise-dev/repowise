builtin_providers = ['gemini', 'openai', 'anthropic', 'deepseek', 'kimi', 'opencode', 'ollama', 'litellm', 'openrouter', 'codex_cli']
# Update the list to match the web dashboard's provider list
# This change is to address issue #1530
# The discrepancy between the web dashboard and the CLI is due to the missing codex_cli provider
