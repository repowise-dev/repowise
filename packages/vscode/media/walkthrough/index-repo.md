# Index the repository

Repowise reads a `.repowise/` index that lives inside your project. Run
**Repowise: Set Up This Repository** to build it for the current workspace.

The first index pass reads your source files and writes the results locally. Once
it finishes, the Overview view lights up and the rest of the features connect.
