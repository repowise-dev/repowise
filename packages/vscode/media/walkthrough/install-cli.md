# Install the CLI

The extension drives the `repowise` command line tool to index and serve your
repository. Install it once, then run **Repowise: Check Setup** from the Command
Palette to confirm the editor can find it.

If the CLI is installed somewhere off your PATH, point the extension at it with
the `repowise.cliPath` setting.
